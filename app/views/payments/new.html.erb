
  <div class="row py-4">
    <div class="col-md-3"><%= image_tag @order.coach.photo_url, width: '100%' %></div>
    <div class="col">
      <h1>Récapitulatif de paiement</h1>
      <p>Achat de <strong><%= @order.coach.email %></strong> pour <strong><%= humanized_money_with_symbol @order.amount %></strong></p>
      <button id="pay" class="btn btn-primary">Payer</button>
      <script src="https://js.stripe.com/v3/"></script>
      <script>
        const paymentButton = document.getElementById('pay');
        paymentButton.addEventListener('click', () => {
          const stripe = Stripe('<%= ENV['STRIPE_PUBLISHABLE_KEY'] %>');
          stripe.redirectToCheckout({
            sessionId: '<%= @order.checkout_session_id %>'
          });
        });
      </script>
    </div>
  </div>


<div>
  <p>For test: Standard credit card</p>
  <p>4242 4242 4242 4242</p>
</div>